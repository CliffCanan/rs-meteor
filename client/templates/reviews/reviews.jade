template(name="reviews")
  if not buildingReviews.count
    p
      | No reviews yet. Be the first to <a id="review-form-link" href="#review-form-modal" data-toggle="modal">leave a review</a>!
  else
    div.average-rating(itemprop="aggregateRating", itemscope, itemtype="http://schema.org/AggregateRating")
      div
        | Rated
        span.avgRating(itemprop="ratingValue") #{averageRating}
        | /
        span(property="bestRating") 10
        | based on
        span.reviewsCount(itemprop="ratingCount") #{reviewsCount}
        | #{reviewText}
        meta(itemprop="worstRating" content="0")
        meta(itemprop="bestRating" content="10")

    ul.list-unstyled.row.reviews
      each buildingReviews
        li.col-sm-6
          +review

    .row
      .col-xs-12.m-t-10
        a#review-form-link.btn.btn-primary.btn-lg.col-sm-6.col-sm-offset-3.col-md-4.col-md-offset-4(href="#review-form-modal", data-toggle="modal")
          i.fa.fa-fw.fa-pencil.m-r-5
          | Write A Review