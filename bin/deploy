#!/bin/bash

DIRNAME=$(cd `dirname $0` && pwd)

meteor deploy -D rentscene.meteor.com
meteor deploy --settings settings/prod.json rentscene.meteor.com
meteor authorized rentscene.meteor.com --add starfall
meteor authorized rentscene.meteor.com --add wsoul
meteor authorized rentscene.meteor.com --add malyutik
$DIRNAME/logs
