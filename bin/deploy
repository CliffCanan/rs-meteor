#!/bin/bash

DIRNAME=$(cd `dirname $0` && pwd)

meteor deploy --settings settings/dev.json rentscene.meteor.com
meteor authorized rentscene.meteor.com --add starfall
meteor authorized rentscene.meteor.com --add wsoul
$DIRNAME/logs
